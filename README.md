# WhatsApp-style chat viewer

Open `index.html` in any modern browser, choose an exported WhatsApp `.txt` chat file, then choose which participant's messages should appear on the right. It supports both Android exports (`date, time - Name: message`) and iPhone exports (`[date, time] Name: message`).

No server or installation is required; the selected file is processed locally in the browser.
